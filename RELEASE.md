# 发布说明

## 当前状态

网站为静态发布候选版，构建产物位于 `dist/`。页面本身不依赖后端、数据库或环境变量；首屏视频通过 Mux HLS 在线加载。

## 发布前必须确认

1. 对外数据口径：15年 / 70+ / 80亿+。
2. “老舍文艺基金会 AI影视发展专项基金 主任”的公开表述。
3. 电影海报的公开使用授权与展示范围。
4. 正式域名和发布平台。

## 正式域名确定后

- 增加 canonical 与 `og:url`。
- 将 Open Graph 图片改为正式域名绝对地址。
- 补充 `sitemap.xml` 并更新 `robots.txt`。

## 推荐部署

- Netlify、Vercel、Cloudflare Pages：构建命令 `pnpm build`，发布目录 `dist`。
- 普通静态服务器：上传 `dist/` 全部内容。

## 发布后复验

1. Mux 视频正常播放，弱网时深色兜底背景可见。
2. 三张人物图、六张代表项目图和 20 张原海报均加载成功。
3. 电话链接在手机端可唤起拨号。
4. 电影切换、移动端横滑、灯箱开关和键盘操作正常。
